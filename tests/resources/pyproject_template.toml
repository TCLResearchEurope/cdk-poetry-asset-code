[tool.poetry]
name = "test_project"
version = "0.1.0"
description = "A test Poetry project for pytest"
authors = ["Your Name <you@example.com>"]
packages= [{include = "my_package"}]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.25.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
